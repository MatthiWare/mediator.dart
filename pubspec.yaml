name: dart_mediator
description: >
  A simple yet highly configurable Mediator implementation
  that allows sending requests and publishing events.
version: 0.1.0
repository: https://github.com/MatthiWare/event_manager.dart

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  meta: any

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.9
  mocktail: ^1.0.1
